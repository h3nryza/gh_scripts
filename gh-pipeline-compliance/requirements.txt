# gh-pipeline-compliance requirements
# Written by h3nryza

# AWS Lambda / S3 support (optional for local mode)
boto3>=1.34.0

# GitHub App JWT signing (only needed for --auth app)
cryptography>=42.0.0

# YAML parsing for workflow files (stdlib yaml is sufficient, but PyYAML
# is included for any future structured parsing)
PyYAML>=6.0.1

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0
responses>=0.25.0
moto[s3]>=5.0.0
